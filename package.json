{
  "name": "thotsparkz-resort",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit:prod": "npm audit --omit=dev",
    "check": "npm run build && npm run audit:prod"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
